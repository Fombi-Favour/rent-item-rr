import React from 'react';

const Feedback = () => (
  <section className="bg-img-feedback w-full h-screen bg-cover md:bg-center">
    <div className="w-full h-full bg-white/40 flex flex-col items-center justify-center">
      feedback
    </div>
  </section>
);

export default Feedback;
